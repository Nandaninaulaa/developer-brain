# --- Web framework ---
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.4.0
gunicorn==23.0.0
whitenoise==6.7.0
dj-database-url==2.3.0
psycopg2-binary==2.9.10

# --- Config ---
python-dotenv==1.0.1

# --- RAG / knowledge graph ---
chromadb==0.5.15
# Pin posthog below its v3 API rewrite — chromadb 0.5.15 calls the old
# capture(user_id, event, properties) signature, and letting pip resolve
# posthog unpinned grabs the latest (incompatible) version.
posthog<3
sentence-transformers==3.1.1
pypdf==5.0.1
openai==1.51.0
neo4j==5.24.0
